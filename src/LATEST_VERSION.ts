// Consider moving to vercel env config (but this initially has drawback that local env has to be configured as well separately)
export const LATEST_VERSION = '0.5'
export const LATEST_VERSION_PATH = 'latest'
